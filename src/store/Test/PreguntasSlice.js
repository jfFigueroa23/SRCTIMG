/* import { createSlice } from '@reduxjs/toolkit';

export const PreguntasSlice = createSlice({
    name: 'Preguntas',
    initialState: {
        isLoadingCalls: true,
        Preguntas: []
    },
    reducers: {
        onLoadPreguntas: (state, { payload = [] }) => {
            state.isLoadingPreguntas = false;
            state.Preguntas = payload;

        }
    }
});


// Action creators are generated for each case reducer function
export const { onLoadPreguntas } = PreguntasSlice.actions; */